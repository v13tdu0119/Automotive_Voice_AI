package com.sopa.viva_automotive.core.ui.theme

import androidx.compose.ui.graphics.Color

val DarkBackground = Color(0xFF121418) // dark gray, not #000000
val DarkSurface = Color(0xFF1B1E24)
val DarkSurfaceHigh = Color(0xFF262A31)

val DarkOnSurface = Color(0xFFFFFFFF) // 100% white, ~17.4:1
val DarkOnSurfaceMuted = Color(0xB3FFFFFF) // 70% white, ~8.6:1
val DarkOutline = Color(0x4DFFFFFF) // 30% white, ~3.2:1 (UI elements)

val DarkCyan = Color(0xFF7BC7D1) // interactive / primary
val DarkAmber = Color(0xFFE5B171) // warning / attention
val DarkRed = Color(0xFFE07A77) // urgent / error
val DarkGreen = Color(0xFF85BE88) // success / positive

val LightBackground = Color(0xFFF7F8FA)
val LightSurface = Color(0xFFFFFFFF)
val LightSurfaceHigh = Color(0xFFEAEDF1)

val LightOnSurface = Color(0xFF191C20) // ~15.9:1
val LightOnSurfaceMuted = Color(0xB3191C20) // 70% opacity, ~7.8:1
val LightOutline = Color(0x4D191C20) // 30% opacity, ~3.1:1 (UI elements)

val LightCyan = Color(0xFF00707B) // interactive / primary, ~5.6:1 on white
val LightAmber = Color(0xFF8F5F00) // warning / attention, ~5.4:1
val LightRed = Color(0xFFC62828) // urgent / error, ~5.9:1
val LightGreen = Color(0xFF2E7D32) // success / positive, ~5.1:1

val LightCyanContainer = Color(0xFFD5ECEF)
val LightOnCyanContainer = Color(0xFF00363C)
